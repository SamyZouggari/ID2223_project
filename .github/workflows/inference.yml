name: Hourly Inference Pipeline

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch: 

jobs:
  run-inference:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        pip install hopsworks==4.4.2 xgboost pandas scikit-learn
    
    - name: Run inference
      env:
        HOPSWORKS_API_KEY: ${{ secrets.HOPSWORKS_API_KEY }}
      run: python src/utils/inference/inference_pipeline.py